{
  "name": "ngo-management-android",
  "version": "1.0.0",
  "private": true,
  "description": "Android wrapper for the NGO management client application",
  "scripts": {
    "sync": "npx cap sync android",
    "copy": "npx cap copy android",
    "open:android": "npx cap open android",
    "build:apk": "npm run sync && cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "8.4.1",
    "@capacitor/core": "8.4.1"
  },
  "devDependencies": {
    "@capacitor/cli": "8.4.1"
  },
  "engines": {
    "node": ">=22"
  }
}
